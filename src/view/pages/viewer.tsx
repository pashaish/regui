import React from 'react';
import { Editor } from '../components/editor';
import { Menu } from '../components/menu';

export const Viewer = () => {
    return <div>
        <Menu />
        <Editor />
    </div>
}
